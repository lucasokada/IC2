//INCOMPLETO

#include <stdio.h>
#include <stdio.h>
#include <string.h>

void percorre_arquivo(FILE *file)
{
    char c;
    
    file = fopen("arquivo.txt", "r");

    while(fgetc(file) != EOF){
        
    }
}

int main()
{
    int i;
    char palavra_minuscula[20] = {"senha"};
    char palavra_maiuscula[20] = {"SENHA"};

    FILE *file;

    printf("\n");
}